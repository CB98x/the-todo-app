I have a GitHub repo called `the-todo-app` that I'm building in versions
as I progress through a 12-week self-study SWE program. The repo lives
at ~/TDL/the-todo-app/ on my machine and is pushed to GitHub.

Current structure:
the-todo-app/
├── README.md          (top-level — lists all versions)
├── .gitignore
├── v1-cli/
│   ├── todo.py
│   └── README.md      (notes for v1)
├── v2-.../            (and so on as I add versions)

I want to add a new version. Here's what's new in it:

- Version number: v[FILL IN]
- Short name for the folder (e.g. "v2-with-tests", "v3-fastapi"): [FILL IN]
- What's new compared to the previous version: [FILL IN — bullet points
  are fine]
- What I learned building it: [FILL IN]
- Code location on my machine right now: [FILL IN, e.g. ~/TDL/week4/]

Please help me:
1. Move the code into the right folder under the-todo-app/
2. Write a README.md for the new version folder, in the same style as
   the v1 README (what it does, how to run, what I learned)
3. Update the top-level README.md to list the new version
4. Update .gitignore if the new version needs anything excluded (new
   dependencies, build artifacts, etc.)
5. Give me the exact git commands to commit and push, with a good
   commit message

I use Git Bash on Windows and have `gh` CLI installed and authenticated.
Give me commands I can copy-paste in order. Stop after each major step
so I can confirm before continuing.

Same rules as always: explain what each command does so I'm not blindly
copying. If you spot something that should be cleaner (folder name,
README structure, commit message), push back — don't just rubber-stamp
my choices.